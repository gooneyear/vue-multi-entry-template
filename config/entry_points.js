const entry_points = [
  'student',
  'manager',
  'teacher'
];

const commons = ['vendor', 'manifest'];

const src_dir = 'src/pages';

const dist_dir = 'dist';

module.exports = {
  entry_points,
  commons,
  src_dir,
  dist_dir,
}
